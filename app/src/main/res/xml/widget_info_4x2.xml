<?xml version="1.0" encoding="utf-8"?>
<!--
  4×2 横条（DESIGN §3.6）：日期行 + 焦点课（宽版焦点卡）。
  2026-09-19 条目调整：2×4 竖条改为 4×2 横条（用户拍板）；2×4 形态保留为
  4×2 竖向拉伸后的兜底档（渲染按实际尺寸分档，见 WidgetSize / widgetSizeFor）。
  minWidth/minHeight 是 Android 11 及以下的回退尺寸（按格位取整），
  targetCellWidth/Height 是 Android 12+ 的格位声明；两套都给，覆盖各版本 ROM。
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="400dp"
    android:maxResizeHeight="250dp"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="0"
    android:description="@string/widget_description"
    android:previewImage="@mipmap/ic_launcher"
    android:initialLayout="@layout/glance_default_loading_layout" />
